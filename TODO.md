# TODO

- [x] Исправить запуск `mip run` на Windows 10 при путях с пробелами: переписать `lib/commands/run.js` (убрать `script.split(' ')`, корректно запускать через `cmd.exe /c`).

- [ ] Обновить `lib/core/workspace.js` и `lib/commands/workspaces.js` (убрать небезопасный `execSync` с `cd "${ws}" && ...`, заменить на `spawn/exec` с безопасным quoting).

- [ ] При необходимости проверить/починить `lib/commands/exec.js` (убрать `shell:true` или обеспечить корректные аргументы).
- [ ] Выполнить сборку/линт (без запуска тестов, как просили) и убедиться, что нет синтаксических ошибок.

