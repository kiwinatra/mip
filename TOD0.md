# TODO - Test improvements

- [ ] Add global Mocha test logger that logs every `describe`/`it` start and result (pass/fail) with timing
- [ ] Wire logger into `npm test` by adding `--require test/helpers/test-logger.js` to `test:all` script
- [ ] Run `npm test` and confirm all tests pass
