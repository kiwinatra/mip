mip 2.2 update (faster whan robtop :3)

This update was centred to fixing and finishing features.

New features:

[+] Added hello command, which works like neofetch
[+] Alias command, centered to work with plugins (not pe, normal alias)
[+] Mip server now includes blog and renamed to **page**

Fixed features:

[~] Completed features into all files
[~] Fixed plugin's API
[~] maybe fixed completion
[~] Fixed language command error

Known issues:

[!] mip-lang plugin is not applying languages.

