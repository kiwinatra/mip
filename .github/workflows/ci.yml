name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  check:
    name: Проверка кода
    runs-on: ubuntu-latest
    
    steps:
      - name: Клонирование репозитория
        uses: actions/checkout@v4
      
      - name: Установка Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Установка зависимостей
        run: npm ci || npm install
      
      # ============================================================
      # 1. ЛИНТЕР — если ошибка → СТОП
      # ============================================================
      - name: 🔍 Линтер (ESLint)
        id: lint
        continue-on-error: false
        run: |
          echo "::group::📋 Запуск линтера"
          START=$(date +%s%N)
          npm run lint
          END=$(date +%s%N)
          DURATION=$(( ($END - $START) / 1000000 ))
          echo "duration=${DURATION}ms" >> $GITHUB_OUTPUT
          echo "::endgroup::"
      
      
      
      # ============================================================
      # 3. АУДИТ БЕЗОПАСНОСТИ — НЕ ЛОМАЕТ СБОРКУ
      # ============================================================
      - name: 🔒 Аудит безопасности (предупреждение)
        id: audit
        continue-on-error: true
        run: |
          echo "::group::🔒 Аудит безопасности"
          START=$(date +%s%N)
          npm audit --omit=dev --audit-level=high || true
          END=$(date +%s%N)
          DURATION=$(( ($END - $START) / 1000000 ))
          echo "duration=${DURATION}ms" >> $GITHUB_OUTPUT
          echo "::endgroup::"
      
      # ============================================================
      # 4. ЕСЛИ ДОШЛИ СЮДА → ВСЁ ОК
      # ============================================================
      - name: ✅ Всё прошло
        if: success()
        run: echo "🎉 Все проверки пройдены! Можешь мержить."

      # ============================================================
      # 5. ЕСЛИ ОШИБКА → ОТПРАВЛЯЕМ В TELEGRAM
      # ============================================================
      - name: 📱 ОШИБКА — уведомление в Telegram
        if: failure()
        env:
          TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          COMMIT: ${{ github.event.head_commit.message }}
          AUTHOR: ${{ github.actor }}
          REPO: ${{ github.repository }}
          BRANCH: ${{ github.ref_name }}
          RUN_ID: ${{ github.run_id }}
          SERVER: ${{ github.server_url }}
          SHA: ${{ github.sha }}
        run: |
          if [[ "${{ steps.lint.outcome }}" == "failure" ]]; then
            STEP="🔍 Линтер (ESLint)"
            FIX="Проверь синтаксис, неиспользуемые переменные, отступы"
            CMD="npm run lint --fix"
          elif [[ "${{ steps.test.outcome }}" == "failure" ]]; then
            STEP="🧪 Тесты"
            FIX="Запусти тесты локально: npm test"
            CMD="npm test -- --verbose"
          else
            STEP="❌ Неизвестная ошибка"
            FIX="Смотри логи"
            CMD=""
          fi
          
          MSG="❌ *MIP CI — ОШИБКА*

          📦 *Репозиторий:* ${REPO}
          🌿 *Ветка:* ${BRANCH}
          👤 *Автор:* ${AUTHOR}
          📝 *Коммит:* ${COMMIT:0:60}...

          ──────────────
          🚨 *Упало на:* ${STEP}

          💡 *Что делать:*
          ${FIX}

          🔧 *Команда для фикса:*
          \`${CMD}\`

          ──────────────
          🔗 *Логи:* ${SERVER}/${REPO}/actions/runs/${RUN_ID}"
          
          if [[ -n "$TOKEN" && -n "$CHAT_ID" ]]; then
            curl -s -X POST "https://api.telegram.org/bot${TOKEN}/sendMessage" \
              -H "Content-Type: application/json" \
              -d "$(cat <<EOF
          {
            "chat_id": "${CHAT_ID}",
            "text": "${MSG}",
            "parse_mode": "Markdown",
            "disable_web_page_preview": true,
            "reply_markup": {
              "inline_keyboard": [
                [
                  {"text": "📋 Логи", "url": "${SERVER}/${REPO}/actions/runs/${RUN_ID}"},
                  {"text": "📝 Коммит", "url": "${SERVER}/${REPO}/commit/${SHA}"}
                ]
              ]
            }
          }
          EOF
          )"
          fi
          
          exit 1